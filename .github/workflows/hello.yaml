name: Hello World!

on:
  push:
    branches:
      - master

jobs:
  hello_world_job:
    runs-on: ubuntu-20.04
    steps:
      - name: Whoami
        run: |
          whoami
      - name: Date
        run: |
          date
      - name: Directory Listing
        run: |
          ls -lhisa

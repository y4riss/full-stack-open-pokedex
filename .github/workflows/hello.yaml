name: Hello World!

# i want to trigger this workflow each minute on the minute
on:
  schedule:
    - cron: '*/1 * * * *'

jobs:
  hello_world_job:
    runs-on: ubuntu-20.04
    steps:
      - name: Say hello
        run: |
          echo "Hello gihub :)"
